KafkaServer {
  org.apache.kafka.common.security.plain.PlainLoginModule required{% for user in credentials %}

  username="{{ user.username }}" password="{{ user.password }}"{% endfor %};
};

controller.KafkaServer {
  org.apache.kafka.common.security.plain.PlainLoginModule required{% for user in credentials %}

  username="{{ user.username }}" password="{{ user.password }}"{% endfor %};
};
