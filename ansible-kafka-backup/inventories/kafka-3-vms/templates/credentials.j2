KafkaServer {
  org.apache.kafka.common.security.scram.ScramLoginModule{% for user in credentials %}

  username="{{ user.username }}" password="{{ user.password }}"{% endfor %};
};

controller.KafkaServer {
  org.apache.kafka.common.security.scram.ScramLoginModule{% for user in credentials %}

  username="{{ user.username }}" password="{{ user.password }}"{% endfor %};
};

KafkaClient {
  org.apache.kafka.common.security.scram.ScramLoginModule{% for user in credentials %}

  username="{{ user.username }}" password="{{ user.password }}"{% endfor %};
};