---
- name: Assign node roles based on inventory group
  tags:
    - containers_run
  set_fact:
    groups: "{{ groups | default({}) | combine({item: hostvars[item]['group_names'] | difference(['cluster'])}) }}"
  with_items: "{{ startup_order }}"
  run_once: true

- name: Run containers according to assigned roles
  tags:
    - containers_run
  delegate_to: "{{ item }}"
  run_once: true
  shell: >
    {% if 'controllers' in groups[item] %} {{ docker_command_controllers_run }}
    {% elif 'brokers' in groups[item] %} {{ docker_command_brokers_run }}
    {% elif 'connect' in groups[item] %} {{ docker_command_connect_run }}
    {% else %} echo 'Undefined group, no command executed'
    {% endif %}
  with_items: "{{ startup_order }}"
  when: groups[item] is defined


# old variant - single command for all containers
#- name: Run docker containers in startup order
#  tags:
#    - containers_run
#  shell: "{{ docker_command_controllers_run }}"
#  delegate_to: "{{ item }}"
#  with_items: "{{ startup_order }}"
#  run_once: true  # Ensure task runs only once on localhost
