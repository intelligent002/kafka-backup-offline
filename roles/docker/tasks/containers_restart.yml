---
- name: Restart Kafka containers on each cluster node
  tags:
    - containers_restart
  when:
    - "item.role in group_names"
  docker_container:
    name: "{{ item.name }}"
    state: started
    restart: true
  loop: "{{ kafka_services }}"
  loop_control:
    label: "{{ item.role }}"
