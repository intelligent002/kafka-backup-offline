---
- name: Remove Portainer on all nodes
  tags: gui_portainer
  shell: >
    docker volume rm portainer_data
    docker rm -f portainer

- name: Deploy Portainer on all nodes
  tags: gui_portainer
  shell: >
    {{ docker_command_portainer_run }}
